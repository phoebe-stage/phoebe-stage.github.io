export default function Title({textcolour, backgroundcolour}) {
    return (
        <p className={`flex w-1/5 h-1/5 pt-10 pb-10 text-xl text-center justify-center text-${textcolour} bg-${backgroundcolour}`}> testing </p>
  );
  }
  